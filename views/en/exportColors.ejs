<!DOCTYPE html>
<% include ../partials-en/head %>

  <body >
    <% include ../partials-en/header %>
      <main>
        Export colors
        <hr />
        <% for(var i = 0 ; i < exportArray.length ; i++) { %>
          <%=  exportArray[i] %>
            <br />
            <% } %>
              <hr />
      </main>
      <% include ../partials-en/footer %>
  </body>

  </html>